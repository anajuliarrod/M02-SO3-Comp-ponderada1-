<header class="header">
  <% if (showBackButton) { %>
      <button class="back-button" onclick="history.back()">
          ←
      </button>
  <% } %>
  <h1><%= pageTitle %></h1>
  <div class="user-icon">
      👤
  </div>
</header>
